      <nav class="navbar">
         <% if user_signed_in? %>

            <ul class="navbar-brand navbar-menu d-flex justify-content-between">
                <li class="nav-item"> <%= @current_user.name %></li>
                 <li class="nav-item">

                 </li>
                 <li class="nav-item active align-self-end">
                    <%= link_to 'Log out', destroy_user_session_path, method: :delete, class:'my-auto' %>
                </li>
            </ul>

       <% else %>
          <div class="not-signed align-self-start"> 
             <% if alert %> <div class="alert alert-danger"><%= alert %></div> <% end %>
             
          </div>

       </nav>
       <% end %>

        
